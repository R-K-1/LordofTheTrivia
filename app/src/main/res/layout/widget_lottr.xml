<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/widget_margin"
    android:background="@color/colorPrimary">

<!--    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">-->

        <ImageView
            android:id="@+id/widget_launch_categories_grid"
            android:layout_width="@dimen/widget_icon_width"
            android:layout_height="@dimen/widget_icon_height"
            android:layout_weight="1"
            android:src="@drawable/ic_clear_all_black_24dp"
            android:layout_above="@+id/widgetCollectionList"
            android:layout_alignParentStart="true"
            android:layout_alignParentLeft="true"
            android:contentDescription="@string/widget_launch_categories"/>

        <ImageView
            android:id="@+id/widget_launch_all_sets_grid"
            android:layout_width="@dimen/widget_icon_width"
            android:layout_height="@dimen/widget_icon_height"
            android:layout_weight="1"
            android:src="@drawable/ic_playlist_play_black_24dp"
            android:layout_above="@+id/widgetCollectionList"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:contentDescription="@string/widget_launch_trivia_sets"/>

        <ListView
            android:id="@+id/widgetCollectionList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true">
        </ListView>

<!--
    </ScrollView>
-->

<!--    <ImageView
        android:id="@+id/widget_launch_scores_grid"
        android:layout_width="@dimen/widget_icon_width"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:src="@drawable/ic_menu_gallery"/>-->

</RelativeLayout>